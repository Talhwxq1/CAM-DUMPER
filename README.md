# CAM-DUMPER
This Tool a Camera Hacking tool
you need this modules
```bash
pkg update -y && pkg upgrade -y && pkg install -y php cloudflared termux-api wget curl python python-pip && termux-setup-storage
```
and 
```bash
git clone 
